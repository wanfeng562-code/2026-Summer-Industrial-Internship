# 成员 C 三角色端到端验收清单

> 状态：登录态、请求层、核心页面、核心 API 验收脚本均已完成并通过静态构建；实际执行等待本机 MySQL/后端和 DashScope 环境。

## 1. 验收环境

- [ ] 已执行成员 C 数据库迁移，`ticket_operation_log`、`faq` 和 `after_sale_policy.sla_hours` 存在。
- [ ] 后端使用本机 `DB_PASSWORD` 启动；AI 用例另配置本机 `DASHSCOPE_API_KEY`。
- [ ] 前端 Node 版本满足 `package.json`（当前 `22.13.1` 仍需升级）。
- [x] 已执行 `npm ci`，且 `npm run build` 通过（2026-07-28）。
- [x] 已提供 `scripts/e2e-core.ps1`，覆盖登录、订单、创建、转人工、接单、回复、解决、关闭、用户列表和统计。
- [ ] USER、AGENT、ADMIN 三类账号均可登录，浏览器请求携带 Bearer Token。
- [ ] 准备两个不同普通用户及各自订单，用于验证越权隔离。

## 2. 核心闭环

| 编号 | 角色与操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| E2E-01 | USER 查询订单并从本人订单创建工单 | 创建成功，初始为 `AI_PROCESSING`，生成 SLA、首条消息和创建日志 | - [ ] |
| E2E-02 | USER 用他人订单创建工单 | HTTP 403，数据库不新增工单 | - [ ] |
| E2E-03 | USER 在详情请求转人工 | 转为 `MANUAL_REVIEW`，出现系统消息和转人工日志 | - [ ] |
| E2E-04 | 两个 AGENT 同时接取同一工单 | 仅一个成功；另一个 HTTP 409 | - [ ] |
| E2E-05 | 未接单 AGENT 回复工单 | HTTP 403，不新增消息 | - [ ] |
| E2E-06 | 已接单 AGENT 回复并标记解决 | 消息可见，状态转为 `RESOLVED`，写入解决时间和日志 | - [ ] |
| E2E-07 | USER 对已解决工单补充消息 | 状态回到 `MANUAL_REVIEW`，保留原处理客服 | - [ ] |
| E2E-08 | AGENT 再次解决并关闭 | 仅 `RESOLVED -> CLOSED` 成功，写入关闭时间和日志 | - [ ] |
| E2E-09 | 任意角色向已关闭工单发消息 | HTTP 409，不新增消息 | - [ ] |
| E2E-10 | ADMIN 分配/改派人工工单 | 目标必须是有效 AGENT，详情显示新客服并写日志 | - [ ] |

## 3. 策略、SLA 与 FAQ

| 编号 | 操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| E2E-11 | ADMIN 新增、修改、禁用售后策略 | 列表即时刷新；非 ADMIN 请求返回 403 | - [ ] |
| E2E-12 | 创建可命中策略的分类工单 | SLA 使用策略 `slaHours`，未命中时使用优先级默认值 | - [ ] |
| E2E-13 | 构造临近/超过 SLA 的测试数据 | 分别写入预警/升级标记和日志，优先级最多升到 `URGENT` | - [ ] |
| E2E-14 | ADMIN 管理 FAQ，三角色关键词检索 | 只返回启用且未删除 FAQ；未登录请求失败 | - [ ] |

## 4. AI 与安全

| 编号 | 操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| E2E-15 | 普通 AI 对话 | 返回中文建议；源码、响应和日志不出现 Key | - [ ] |
| E2E-16 | AI SSE 对话并停止生成 | 页面逐段显示；停止后连接被取消且可再次发送 | - [ ] |
| E2E-17 | USER 诱导 AI 查询他人订单/工单 | 工具返回 403/拒绝，响应不包含他人数据 | - [ ] |
| E2E-18 | 诱导 AI 执行退款、关单或改优先级 | AI 明确不能执行；数据库无业务写入 | - [ ] |
| E2E-19 | 暂停/断开 DashScope | 普通接口提示服务不可用；工单 AI 失败后安全转人工并记录日志 | - [ ] |
| E2E-20 | AI 页面点击“转人工/创建工单” | 跳转创建页并带入最近问题，仍由用户确认订单和表单 | - [ ] |

> 2026-07-29 记录：E2E-15 的真实普通 AI 请求已成功返回；E2E-16 先后复现旧 SSE 无事件帧、协议字段进入正文和 Sa-Token 异步收尾异常。原生 SSE 单次编码、UTF-8 与异步鉴权修复、51 项后端测试和前端生产构建已通过，待重启 IDEA 后端后重新执行 E2E-16，未复测前不勾选。

## 5. 问题记录模板

| 日期 | 用例 | 严重度 | 现象 | 负责人 | 修复提交 | 复测 |
| --- | --- | --- | --- | --- | --- | --- |
| 待填写 |  | 阻塞/高/中/低 |  | A/B/C |  | - [ ] |

验收完成后，将执行日期、环境、截图/日志位置、失败原因和修复提交补充到本文件，并同步勾选 `系统开发进度与协作约定.md` 的 C4 任务。

核心 API 冒烟命令：

```powershell
.\scripts\e2e-core.ps1
```

后端已配置有效 DashScope Key 时，可追加真实普通对话：

```powershell
.\scripts\e2e-core.ps1 -TestAi
```
