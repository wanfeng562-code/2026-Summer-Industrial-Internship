# AI 智能客服工单处理系统：三人协作任务分工

> 开发基线：后端使用 `ticketsystem/`，前端使用 `ticket-vue/`；两者已替换为 2026-07-28 的老师代码快照。
>
> 旧日期目录只用于学习和对比，不继续修改。三人以功能模块为边界协作：每个模块应包含后端接口、前端页面（如需要）、测试用例和交接说明。

## 1. 当前系统与共同目标

当前项目已具备 Spring Boot 3、MyBatis-Plus、Sa-Token、Vue 3、Element Plus、Axios 和 Pinia 的基础代码。7-28 快照新增了 Spring AI Alibaba/DashScope、工单自动分类和 AI 自动回复、消息发送联调、订单归属分页，以及登录/创建工单 JSON 接收；但它仍是课堂版 AI 接入，状态动作按钮未接通、AI 工具尚未按权限隔离、模型 Key 配置和 SQL 存在待修复问题，不能视为完整闭环。

共同目标是完成可演示的智能工单闭环：

```text
用户登录 -> 查询订单/创建工单 -> 查看工单与消息
客服登录 -> 查看待处理工单 -> 接单/回复/解决/关闭
管理员登录 -> 管理用户和业务数据 -> 查看统计
AI 客服 -> 回答常见问题 -> 复杂问题转人工或生成工单
```

## 2. 成员 A：后端平台、账号权限与订单模块

成员 A 的重点是让后端接口安全、稳定、契约统一，并提供前端和 AI 模块可靠的数据基础。

> **完成状态（2026-07-28）**：A1、A2、A3 的后端实现、SQL、启动说明、接口文档和 Postman 集合已完成；Maven 打包成功，16 个自动化测试全部通过。B、C 可分别开始真实前端和工单流转联调。证据见 `docs/成员A开发工作记录.md`。

### 模块 A1：账号、认证与权限

- 统一登录接口的 JSON 请求体；返回 `code`、`msg`、`data` 格式。
- 完成登录、退出、注册、个人资料查询/修改。
- 使用 BCrypt 等方式加密密码，移除明文密码比对。
- 完善 Sa-Token：登录失效、未登录、无权限的统一异常响应。
- 根据 `USER`、`AGENT`、`ADMIN` 实现菜单/API 权限与数据范围控制。
- 普通用户只能访问自己的订单和工单；客服处理分配给自己或可领取的工单；管理员可查看全量数据。

**验收结果**：三种演示账号登录后可获得正确 Token 与权限；越权访问订单/工单会被拒绝；未登录请求返回 401。

### 模块 A2：订单与基础业务接口

- 修复订单分页、订单详情接口，并按当前用户做数据过滤。
- 明确订单列表、订单详情的请求/响应 DTO，供创建工单页面使用。
- 创建工单时校验关联订单存在且属于当前用户。
- 修复 DTO 校验：`Long` 使用 `@NotNull`，字符串使用 `@NotBlank`；所有可选更新字段使用 null 安全判断。
- 统一请求格式：登录、创建工单等接口统一接收 JSON，避免前后端表单/JSON 不匹配。

**验收结果**：普通用户可以查询自己的订单、选择订单创建工单；传入空参数、错误订单、他人订单都有正确错误提示。

### 模块 A3：后端工程与接口交付

- 将数据库账号、模型 Key 等敏感配置移出提交内容，使用环境变量或本地配置。
- 整理建表 SQL、样例数据和后端启动说明。
- 维护接口文档及 Postman/Apifox 测试集合，交付给成员 B、C。
- 为登录、订单、权限、工单创建等关键 Service 增加测试。

**交接内容**：接口地址、方法、请求 JSON、响应 JSON、角色权限、样例账号和测试数据。

## 3. 成员 B：前端基础、用户端工单体验与数据展示模块

成员 B 负责将当前 Vue 页面从“页面骨架”变为可稳定操作的用户端，并提供统一的前端基础能力。

> **交付状态（2026-07-28）**：已从 `origin/main` 的 `9ab2e83` 筛选 B 责任范围并整合到正式 `ticket-vue/`；后续已补齐管理员用户/客服列表和按角色范围的真实工作台统计。请求层、登录态、路由、页面和通用组件均通过 TypeScript 与 Vite 生产构建；仅真实后端浏览器验收待执行，详见 `docs/成员B开发工作记录.md`。

### 模块 B1：前端工程、请求层与登录态

- 安装依赖并保证 `npm run build`、类型检查通过，修复当前模板和 TypeScript 错误。
- 完善 Axios 请求封装：超时、Token 注入、401 清理登录态、403/404/500 提示。
- 完善 Pinia 用户状态持久化，统一使用 `nickname`、`roles`、`permissions` 等正确字段。
- 完成路由守卫：未登录不能进入 `/home/**`；退出或 Token 失效后跳回登录页。
- 统一所有 API TypeScript 类型，修复订单列表应为 `R<Page<Orders>>` 等类型错误。

**验收结果**：登录、刷新页面、退出、Token 过期均有正确行为；所有浏览器业务请求经 `/api` 代理并携带 Bearer Token。

### 模块 B2：用户端订单与工单页面

- 完成工单列表：真实分页、状态/分类/优先级中文显示、加载态、空态、错误态。
- 完成创建工单页：加载自己的订单、表单校验、提交成功后刷新列表。
- 完成订单管理页：订单列表、订单详情、与创建工单入口联动。
- 修复菜单与路由不一致：新增必要页面或暂时隐藏未实现菜单；修正错误路径。
- 完成工作台：显示当前用户工单总数、待处理数、处理中数、已解决数和分类概览。

**验收结果**：普通用户能够独立完成“登录 -> 查看订单 -> 创建工单 -> 工单列表 -> 查看详情 -> 退出”。

### 模块 B3：通用后台界面与管理端展示

- 维护后台 Layout、侧边菜单、Header、面包屑和角色菜单显示。
- 实现管理员可用的用户/客服列表展示和基础工单统计展示页面。
- 为成员 C 的 AI 对话模块提供页面容器、导航入口、通用消息样式和错误提示组件。
- 统一 Element Plus 的表单、表格、分页、Tag、确认框和消息提示使用规范。

**交接内容**：页面路由表、页面截图/操作说明、可复用组件说明、构建结果。

## 4. 成员 C：工单流转、售后策略、AI 客服与验收模块

成员 C 负责使工单从创建到关闭真正流转，并在稳定流程上接入 AI 能力；同时承担端到端验收。

### 模块 C1：工单详情、消息与处理流转

- 完整负责工单详情页和相关 API：详情加载、消息列表、发送消息、接单、解决、关闭。
- 封装并接入 `POST /tickets/{id}/messages`、`PUT /tickets/{id}`。
- 明确并实现状态机，例如 `AI_PROCESSING -> MANUAL_REVIEW -> RESOLVED -> CLOSED`；禁止不合理的状态跳转。
- 接单时写入 `agentId`；客服回复、用户补充消息后按规则更新工单状态。
- 增加状态变化/操作日志，保证可以追溯“谁在何时进行了什么操作”。

**验收结果**：客服可以看到需要人工处理的工单，接单、回复、解决、关闭后，用户刷新详情页能看到客服、消息和最终状态。

### 模块 C2：售后策略与 SLA

- 完成售后策略管理：分类、条件、自动动作、回复模板、优先级、启停。
- 实现基础 SLA：创建工单时计算截止时间；到期前预警、到期后升级优先级或标记。
- 在工单详情和列表中显示 SLA 状态、处理客服、处理进度。
- 为后续 AI 自动处理提供“按策略决定自动回复/自动处理/转人工”的确定性业务规则。

**验收结果**：管理员能配置至少一条策略；不同分类工单可以匹配对应的处理规则和 SLA 展示。

### 模块 C3：AI 智能客服与知识库

- 配置 Spring AI Alibaba 与阿里百炼；API Key 只从环境变量读取，绝不提交到 Git。
- 先完成普通对话，再完成流式 SSE 回复。
- 设计 Prompt 模板：客服身份、支持范围、分类规则、无法处理时转人工、禁止伪造订单信息。
- 将工单标题、描述、历史消息、订单信息作为 AI 上下文；保存 AI 回复与处理日志。
- 使用 Tool Calling 实现只读工具：查询订单、工单、售后策略；工具调用仍由后端校验权限。
- 完成 AI 客服页面：会话消息、流式输出、停止生成、转人工/创建工单入口、异常提示。
- 完成 FAQ 基础管理与关键词检索；有余力再扩展 RAG/向量检索。

**验收结果**：简单问题可以得到 AI 回复；复杂问题能被转人工或生成工单；AI 不能越权读取他人订单，也不会泄露 Key。

### 模块 C4：端到端验收与流程文档

- 编写并维护端到端测试用例、测试数据和问题清单。
- 验收用户、客服、管理员三种角色的关键路径。
- 维护工单状态机和 AI 处理流程图，供项目文档与答辩使用。

**核心验收用例**：

```text
1. user_wang 登录，选择自己的订单，创建“物流异常”工单。
2. user_wang 在列表和详情页看到工单和首条消息。
3. agent_zhang 登录，查看人工处理工单，接单并回复。
4. agent_zhang 将工单标记为已解决，再关闭。
5. user_wang 重新登录，能看到客服消息和 CLOSED 状态。
6. 管理员可以查看全量工单和统计；未登录与越权请求均被拒绝。
```

## 5. 模块依赖与协作方式

| 提供方 | 提供内容 | 使用方 |
| --- | --- | --- |
| A | 稳定的认证、订单、工单基础接口与 API 文档 | B、C |
| B | 可登录、可创建工单、可显示数据的用户端和通用 UI | A、C |
| C | 工单处理规则、策略、AI 服务、端到端验收用例 | A、B |

- A 修改 API 契约前，先在组内同步请求/响应样例；B、C 不根据猜测对接。
- B 负责通用请求层、路由、Layout、用户端列表/创建页；C 负责工单详情、消息动作、AI 页面，避免同时编辑同一页面。
- C 的 AI/策略模块需要新接口时，与 A 先确定接口和数据表，再独立实现。
- 所有人完成模块后都要自测，并在每日进度表记录“完成内容、提交号、问题、明日计划”。

## 6. 团队共同约定

- 不提交 `node_modules`、`target`、压缩包、真实 API Key、个人 IDE 临时文件。
- 每个功能提交前至少运行一次相关构建、接口测试或端到端测试。
- 新接口必须同步更新：后端 DTO/异常处理、前端类型/API、接口文档和权限说明。
- 新页面必须包含路由、菜单权限、加载态、空态和错误态。
- AI 输出只能作为业务输入，必须经权限和规则校验后才可以改变工单、策略或数据库数据。

## 7. 全量需求补充分工（2026-07-29）

- 成员 A：账号在线/强制下线/密码重置、坐席组与成员、动态工单分类、权限和 MySQL 8.0 可重复迁移。
- 成员 B：系统配置、账号运维、工单扩展动作、AI 历史、服务报表、FAQ 导入导出与语义配置的全部前端交互。
- 成员 C：工单筛选/导出/跟进/驳回/归档/优先级，AI 会话持久化和内容拦截，FAQ 大模型语义匹配，满意度与组长监管报表。

上述任务已经并入 `系统开发进度与协作约定.md` 的 A/B/C 任务表。代码完成不等于真实环境验收完成；数据库迁移、三角色浏览器流程、CSV 中文以及真实 DashScope 语义质量仍须按验收清单执行并记录。

## 8. 联调数据补充（2026-07-29）

- A 负责维护 `seed_rich_demo_data.sql` 的账号、订单编号、事务和幂等性。
- B 使用扩充数据验证长列表、分页、筛选、空态切换、工作台图表和管理员配置页面。
- C 使用全部工单状态、操作日志、满意度、FAQ、策略和 AI 历史验证状态机、报表及只读 Tool Calling。
- 扩充脚本必须在 `migrate_full_requirements.sql` 后执行，只允许追加独立前缀数据，不允许清空成员已有开发数据。
- 当前脚本已在临时 MySQL 8.0.42 连续执行两次：16 个账号、30 个订单、24 个工单、78 条消息、87 条操作日志和 6 个 AI 会话保持不重复。
