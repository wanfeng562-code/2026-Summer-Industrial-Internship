# AI 智能客服工单处理系统：开发进度与协作约定

> 开发基线：后端使用 `ticketsystem-7-21/ticketsystem`，前端使用 `ticket-vue-7-24/ticket-vue`。旧日期目录仅作学习、对比与回溯，不继续修改。
>
> 使用方式：完成一项后，将对应的 `- [ ]` 改为 `- [x]`，并在括号中补充日期、提交号或接口文档链接。没有依赖的任务可直接并行；标注“必须等待”的任务，在前置模块完成并验收前不得进入真实开发与合并。

## 1. 当前目标

完成一个可演示的智能工单闭环：

```text
用户登录 -> 查询订单 -> 创建/补充工单 -> 查看处理进度
客服登录 -> 查看待处理工单 -> 接单/回复 -> 解决/关闭
管理员登录 -> 查看全量数据与统计 -> 管理策略
AI 客服 -> 常见问题回答 -> 转人工或生成工单
```

## 2. 团队共同约定

### 2.1 接口与数据约定

- [ ] 所有新增或改造的业务接口统一使用 JSON 请求体；后端使用 `@RequestBody` 接收，前端 Axios 保持 `application/json`。（A 负责落实，B、C 复核）
- [ ] 所有接口统一返回 `code`、`msg`、`data`；错误码至少区分 400、401、403、404、500。（A）
- [ ] 登录 Token 统一放入 `Authorization: Bearer <token>`；前端统一由请求拦截器注入。（A、B）
- [ ] 统一并记录核心 DTO：用户、订单、工单列表、工单详情、工单消息、工单更新。（A、C 定稿；B 同步类型）
- [ ] `Long` 类型使用 `@NotNull`，字符串使用 `@NotBlank`；可选更新字段须进行 null 安全判断。（A、C）
- [ ] 所有接口变更须同步更新接口文档、前端类型和示例请求/响应。（修改接口者）

### 2.2 工单流程约定

- [ ] 确认状态枚举和允许的状态转换：`AI_PROCESSING -> MANUAL_REVIEW -> RESOLVED -> CLOSED`；禁止越级或逆向变更。（C 主责，A 复核）
- [ ] 明确角色数据范围：用户仅看自己的订单/工单；客服处理已分配给自己的或可领取的工单；管理员可查看全量数据。（A、C）
- [ ] 明确接单、回复、用户补充消息、解决、关闭时的状态变化、操作人和日志规则。（C）
- [ ] 确认三类演示账号、角色和可复现的订单/工单测试数据。（A）

### 2.3 协作与提交约定

- [ ] 使用功能分支开发；提交信息写明模块和动作，例如 `feat(ticket): add claim endpoint`。（全员）
- [ ] 不提交 `node_modules`、`target`、压缩包、真实 API Key、个人 IDE 临时文件。（全员）
- [ ] 每项任务完成前至少执行相关构建、接口测试或页面操作测试，并在本表勾选时注明结果。（全员）
- [ ] 发生接口、数据库表或状态机变更前，先在组内同步；避免 A 与 C 同时修改同一个后端类。（全员）

## 3. 依赖标记说明

| 标记 | 含义 | 执行要求 |
| --- | --- | --- |
| **可独立开发** | 不依赖其他成员的未完成代码 | 可立即开始并独立提交。 |
| **需先约定接口** | 可先实现页面、DTO、Mock 或单元测试 | 开始前必须获得接口字段、状态或权限约定；真实联调等待提供方接口。 |
| **必须等待** | 缺少前置模块将导致功能方向或数据结构不确定 | 在前置任务勾选完成、提供验收结果前，不开始真实功能开发，也不合并相关代码。 |

## 4. 成员 A：后端平台、账号权限与订单模块

**责任边界**：账号、认证、权限、订单与公共后端规范。A 不直接改动 C 负责的工单流转、消息、策略和 AI 业务实现；若需公共 DTO 或权限变更，应先同步 C。

| 进度 | 任务 | 依赖标记 | 前置依赖 / 联调对象 | 完成标准 |
| --- | --- | --- | --- | --- |
| - [ ] | 统一登录、创建类接口的 JSON 请求体与统一响应格式 | 可独立开发 | 联调：B、C | 前端 JSON 请求可被后端正确接收，错误响应格式一致。 |
| - [ ] | 完成登录、退出、注册、个人资料查询/修改 | 可独立开发 | 联调：B | 三类演示账号可登录，Token 与用户信息正确返回。 |
| - [ ] | 密码改用 BCrypt 加密存储和校验 | 可独立开发 | 无 | 数据库不保存明文密码，旧演示账号可迁移或重置。 |
| - [ ] | 完善 Sa-Token 未登录、无权限、登录失效的统一异常响应 | 可独立开发 | 联调：B | 401/403 可被前端正确识别与提示。 |
| - [ ] | 完成角色、菜单/API 权限和数据范围规则 | 需先约定接口 | 先完成：2.2 工单流程约定；联调：C、B | 用户、客服、管理员的越权请求被拒绝。 |
| - [ ] | 修复订单分页、订单详情，并限制用户仅查询自己的订单 | 可独立开发 | 联调：B | 订单分页类型、详情、归属校验正确。 |
| - [ ] | 创建工单时校验订单存在且属于当前用户 | 需先约定接口 | 先约定：C 的创建工单字段与状态初值 | 非本人订单不能创建工单。 |
| - [ ] | 整理 SQL、样例数据、环境变量配置、启动说明 | 可独立开发 | 提供给：B、C | 新成员按文档可启动后端和导入数据。 |
| - [ ] | 发布接口文档与 Postman/Apifox 集合 | 需先约定接口 | 与 C 共同定稿工单 DTO；提供给 B、C | 核心接口都有请求、响应、权限、异常示例。 |
| - [ ] | 为认证、订单、归属校验编写关键测试 | 可独立开发 | 无 | 测试可重复运行并覆盖异常场景。 |

**A 的关键交付物**：可用认证接口、订单接口、角色/权限规则、样例数据、接口文档。

## 5. 成员 B：前端基础、用户端工单体验与数据展示模块

**责任边界**：前端工程、请求层、登录态、Layout、用户订单与工单列表/创建页、通用展示页面。B 不修改 C 的工单详情、消息动作、AI 页面业务逻辑；可提供通用组件和页面容器。

| 进度 | 任务 | 依赖标记 | 前置依赖 / 联调对象 | 完成标准 |
| --- | --- | --- | --- | --- |
| - [ ] | 安装依赖，修复并验证 `npm run build` 与 TypeScript 问题 | 可独立开发 | 无 | 前端生产构建通过。 |
| - [ ] | 完善 Axios：超时、Token 注入、401 清理、错误提示 | 可独立开发 | 联调：A | 所有业务请求经 `/api` 代理并自动携带 Token。 |
| - [ ] | 完成 Pinia 用户状态持久化和路由守卫 | 需先约定接口 | 先约定：A 的登录响应字段；联调：A | 刷新后登录态可恢复；未登录不能访问业务页。 |
| - [ ] | 统一 API TypeScript 类型，修复订单分页和工单 ID 类型问题 | 需先约定接口 | 前置：A、C 发布 DTO 样例 | `R<Page<Orders>>` 等类型与真实接口一致。 |
| - [ ] | 完成后台 Layout、菜单、Header、面包屑与角色菜单显示 | 需先约定接口 | 先约定：A 的角色/权限字段 | 未实现菜单隐藏或标明，路径不再失效。 |
| - [ ] | 完成订单列表、订单详情及“从订单创建工单”入口 | 需先约定接口 | 前置：A 的订单接口完成；联调：A | 用户只能看到自己的订单，并可带入订单创建工单。 |
| - [ ] | 完成工单列表、分页、筛选、状态显示与空/错/加载状态 | 需先约定接口 | 前置：C/ A 定稿工单列表 DTO；联调：C | 列表数据、分页和状态中文映射正确。 |
| - [ ] | 完成创建工单表单、校验与提交后的跳转/刷新 | 必须等待 | 前置：A 完成“订单归属校验 + JSON 契约”；联调：A、C | 创建成功后能在列表和详情中看到首条消息。 |
| - [ ] | 提供工单详情、消息、AI 页面的路由入口、通用消息样式和错误组件 | 可独立开发 | 提供给：C | C 可直接接入页面，不需改动 Layout。 |
| - [ ] | 完成工作台与基础管理统计展示 | 需先约定接口 | 前置：A/C 提供统计接口或统计字段；联调：A、C | 数据真实或明确标注为暂未接入，不保留误导性静态数据。 |

**B 的关键交付物**：可构建前端、可靠登录态与路由、订单/创建/列表闭环页面、可复用 UI 基础。

## 6. 成员 C：工单流转、售后策略、AI 客服与验收模块

**责任边界**：工单详情、消息、接单/解决/关闭、状态机、策略/SLA、AI 服务和端到端验收。C 是工单相关后端实现的主负责人；涉及登录、权限公共规则和订单归属时与 A 协作，不直接覆盖 A 的账号/订单代码。

| 进度 | 任务 | 依赖标记 | 前置依赖 / 联调对象 | 完成标准 |
| --- | --- | --- | --- | --- |
| - [ ] | 定义工单状态机、角色动作和状态转换表 | 可独立开发 | 输出给：A、B | 状态、触发人、允许转换和失败提示形成文档。 |
| - [ ] | 修复工单 DTO 校验、ID 类型、null 安全更新等基础问题 | 可独立开发 | 与 A 同步统一响应/JSON 规范 | 创建、消息、更新接口参数校验正确。 |
| - [ ] | 完成工单详情和消息列表 API | 需先约定接口 | 先约定：A 的用户/权限字段；联调：B | 用户不能读取他人工单，详情含正确消息和操作人。 |
| - [ ] | 完成发送消息、接单、解决、关闭及操作日志 API | 必须等待 | 前置：状态机任务完成；联调：A、B | 每次操作校验角色、状态与归属，并留下可追溯日志。 |
| - [ ] | 完成工单详情页：加载详情、消息列表、回复和状态动作 | 必须等待 | 前置：消息/状态动作 API 完成；联调：B | 客服操作后用户刷新可见消息和最终状态。 |
| - [ ] | 完成售后策略管理、规则匹配与 SLA 截止时间/预警展示 | 必须等待 | 前置：状态机和工单更新 API 完成；联调：B、A | 至少一条策略可匹配不同类别工单并显示 SLA。 |
| - [ ] | 搭建 Spring AI Alibaba/百炼配置，完成普通对话接口 | 需先约定接口 | 先约定：环境变量、模型选择；联调：B | API Key 仅来自环境变量，普通对话可稳定回复。 |
| - [ ] | 完成 AI 对话页面、流式 SSE、停止生成和异常处理 | 必须等待 | 前置：普通对话接口完成；依赖：B 提供页面容器 | 前端可逐字显示、可停止并正确处理异常。 |
| - [ ] | 将订单/工单/消息上下文接入 AI，完成转人工/生成工单 | 必须等待 | 前置：A 的订单归属与 C 的工单流转均完成 | AI 只读取有权数据；复杂问题可受控转人工。 |
| - [ ] | 实现只读 Tool Calling、FAQ 管理和关键词检索 | 必须等待 | 前置：AI 普通对话、订单/工单/策略接口稳定 | 模型不能直接写库或越权查询。 |
| - [ ] | 编写三角色端到端用例、问题清单和流程图 | 需先约定接口 | 前置：A/B/C 核心闭环已联调 | 测试可复现“创建 -> 接单 -> 回复 -> 解决 -> 关闭”。 |

**C 的关键交付物**：可追溯工单闭环、策略/SLA、受控 AI 能力、端到端验收材料。

## 7. 必须等待的关键链路

以下不是全项目的停工点；未列出的任务仍可按上表并行推进。标记为“必须等待”的工作，应在前置项勾选完成并提供可验证结果后再开始真实开发。

| 后续任务 | 必须等待的前置完成项 | 原因 |
| --- | --- | --- |
| B：创建工单真实提交 | A：JSON 契约、订单归属校验 | 否则前端会按不稳定接口开发，且存在越权创建风险。 |
| C：接单/回复/解决/关闭 API | C：状态机与角色动作表 | 未定义状态规则会导致后续数据和页面反复返工。 |
| C：工单详情动作页面 | C：消息与状态动作 API | 先有稳定接口才能完成真实交互；B 可提前提供页面壳和样式。 |
| C：策略/SLA | C：状态机、工单更新 API | SLA 的升级动作依赖可靠的工单状态与处理人。 |
| C：AI 流式页面 | C：普通 AI 对话接口；B：页面容器 | 流式功能应建立在普通对话可用和统一 UI 基础之上。 |
| C：AI 上下文、转人工、Tool Calling | A：订单归属与权限；C：工单流转 | 涉及用户数据读取与业务写入，必须先保证权限和流程安全。 |
| B：真实工作台统计 | A/C：统计口径、统计接口或字段 | 否则只能展示静态占位数据，无法作为验收结果。 |

## 8. 可并行推进的推荐顺序

```text
立即并行
├─ A：认证/订单/JSON 契约/接口文档
├─ B：构建修复/请求层/路由守卫/Layout/页面壳
└─ C：状态机/工单 DTO 修复/详情与消息接口设计

第一轮联调
├─ A -> B：登录、订单、创建工单接口
└─ C -> B：工单列表、详情、消息、状态动作接口

第二轮并行
├─ B：真实页面联调、列表/详情/工作台
├─ C：策略/SLA、普通 AI 对话
└─ A：权限加固、测试、数据与接口维护

最终联调
└─ C 组织三角色端到端验收；A、B 分别修复后端和前端问题
```

## 9. 每次联调前检查

- [ ] 提供方已在本地完成接口测试，并给出示例请求/响应和账号/测试数据。
- [ ] 使用方已同步最新分支、接口文档和 TypeScript 类型。
- [ ] 前端已确认代理地址、Token Header 和错误处理。
- [ ] 后端已确认数据库脚本、环境变量和权限配置。
- [ ] 本次联调覆盖正常路径、未登录、无权限、空数据和非法参数。
- [ ] 联调问题已记录责任人、复现步骤、优先级与修复提交号。

