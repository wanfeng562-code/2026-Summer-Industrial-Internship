# AI 智能客服工单处理系统：开发进度与协作约定

> 开发基线：后端使用 `ticketsystem/`、前端使用 `ticket-vue/`，两者已于 **2026-07-28** 替换为老师提供的 7-28 代码快照；`springAIDemo/` 仍作为独立的 Spring AI 学习示例。旧基线可通过 Git 历史回溯。
>
> 使用方式：完成一项后，将对应的 `- [ ]` 改为 `- [x]`，并在括号中补充日期、提交号或接口文档链接。第 2.1、2.2 节是已完成的项目基线，不构成开发阻塞；标注“需接口实现/联调”的任务可先用 Mock 开发，标注“必须等待”的任务才不得进入真实功能开发与合并。

## 1. 当前目标

完成一个可演示的智能工单闭环：

```text
用户登录 -> 查询订单 -> 创建/补充工单 -> 查看处理进度
客服登录 -> 查看待处理工单 -> 接单/回复 -> 解决/关闭
管理员登录 -> 查看全量数据与统计 -> 管理策略
AI 客服 -> 常见问题回答 -> 转人工或生成工单
```

### 1.1 7-28 基线接收状态

- 已接收：登录与创建工单的 JSON 接收、订单按当前用户分页、创建/补充消息后的 AI 自动分类与回复、AI 回复消息展示、前端发送消息请求、10 秒请求超时，以及 Spring AI Alibaba/DashScope 的基础配置。
- 已接收但**尚不可勾选为完成**：AI 配置、普通 AI 对话、消息动作等仅是课堂版实现，仍须按第 2.1、2.2 节改造、测试并验收。
- 本轮开发的直接优先项：将 DashScope Key 改为环境变量；将 AI 工具收敛为只读且按当前用户做归属校验；修复优先级更新 SQL；统一 `ordersId` 为 `orderId`；让消息请求只传 `content`；接通接单、解决、关闭的真实接口与角色按钮控制。
- 当前没有独立 AI 客服页面或流式 SSE 接口；这些仍按成员 C 的任务表继续完成。

## 2. 团队共同约定

### 2.1 接口与数据约定

> **状态：已确定并生效（2026-07-27）。** 以下规则是前后端联调和新增功能的唯一基准，不再作为任何任务的前置依赖；成员 A 负责后端落实，成员 B、C 分别在前端和工单/AI 模块中遵守。除第 2.3 节的变更流程外，不得自行更改。

1. **通信方式与路径**
   - 浏览器只访问前端代理前缀 `/api`；开发环境由 Vite 转发至后端 `http://localhost:8080`。
   - 查询使用 `GET` + Query 参数；创建使用 `POST` + JSON；完整更新使用 `PUT` + JSON。不得以表单、URL 编码或 Query 参数传递登录密码、工单描述、消息内容等业务请求体。
   - 所有新增/改造的 `POST`、`PUT`、`PATCH` 接口均使用 `Content-Type: application/json`，后端使用 `@RequestBody` 接收。

2. **统一响应与错误规则**

   ```json
   {
     "code": 200,
     "msg": "操作成功",
     "data": {}
   }
   ```

   - 每个接口均返回 `code`、`msg`、`data` 三个字段；无返回对象时 `data` 为 `null`。
   - `code` 与 HTTP 语义保持一致：`200` 成功、`400` 参数校验/格式错误、`401` 未登录或 Token 失效、`403` 无权限或无数据归属、`404` 资源不存在、`409` 状态冲突或重复操作、`500` 未预期服务端异常。
   - 字段校验错误放在 `data.fieldErrors`，格式为 `{ "字段名": "错误提示" }`；前端优先展示该字段提示，其余错误展示 `msg`。

3. **鉴权规则**
   - 登录成功后，后端在 `data` 中返回 `token`、`id`、`username`、`nickname`、`role`、`permissions`；`role` 为 `USER`、`AGENT`、`ADMIN` 之一，`permissions` 为权限字符串数组。
   - 前端 Pinia 只保存上述登录态字段；Axios 请求拦截器统一添加 `Authorization: Bearer <token>`。收到 `401` 时清空登录态并跳转登录页；收到 `403` 时保留登录态并提示无权限。
   - 密码、模型 Key、数据库密码、Token 原文不得写入日志、前端状态、接口响应或 Git。

4. **字段、分页与枚举规则**
   - JSON 字段使用小驼峰命名；主键及关联键统一命名为 `id`、`userId`、`agentId`、`orderId`、`ticketId`，禁止使用 `ordersId` 等复数 ID 命名。
   - 当前项目的 ID 在 JavaScript 安全整数范围内，以 JSON `number` 传输；若未来需要超出该范围，必须通过第 2.3 节统一改为字符串，不能局部修改。
   - 分页响应固定为 `data: { current, size, total, records }`，`current` 从 1 开始，`size` 为正整数。列表页不得自行猜测或拼装总数。
   - 枚举在传输中使用大写代码：分类 `REFUND`、`LOGISTICS`、`DAMAGE`、`INVOICE`、`OTHER`；优先级 `LOW`、`MEDIUM`、`HIGH`、`URGENT`；状态以第 2.2 节为准。中文名称仅用于展示，不能作为提交值或判断条件。
   - 日期时间统一为 ISO-8601 本地时间字符串 `yyyy-MM-dd'T'HH:mm:ss`；金额以接口现有的 decimal/字符串表示，不允许前端浮点计算后回传。

5. **核心 DTO 与校验规则**
   - 登录请求：`{ "username": "", "password": "" }`；创建工单请求：`{ "orderId": 0, "title": "", "description": "", "category": "", "priority": "" }`；发送消息请求：`{ "content": "" }`；工单状态动作由专用接口表达，不允许前端任意提交 `agentId` 或 `status`。
   - 列表、详情和消息对象至少包含其主键、必要关联标识、状态/分类/优先级代码、创建/更新时间；工单详情额外包含 `messages`、`agentId`、`slaDeadline`、`slaWarning`、`slaEscalated`。
   - Java 的 `Long`/其他数值型必填字段使用 `@NotNull`，字符串型必填字段使用 `@NotBlank`；所有更新字段须经过 null 安全校验。前端与后端均要校验必填字段，但以后端校验结果为准。

6. **接口变更控制**
   - 新增字段可选且有默认兼容行为时可直接新增；删除/重命名字段、修改枚举或语义、变更状态规则属于破坏性变更。
   - 破坏性变更必须先更新本文件、接口文档、TypeScript 类型和请求/响应样例，并由 A、B、C 三人确认后才能合并。

### 2.2 工单流程约定

> **状态：已确定并生效（2026-07-27）。** 本节状态机和角色规则不再作为任何任务的前置依赖；成员 C 负责将其实现为代码、消息、操作日志和策略/SLA，成员 B 只按此规则显示状态和开放按钮，成员 A 负责在认证、权限与订单归属层保证规则能够被执行。

1. **角色与数据范围**
   - `USER`：仅可查询自己的订单与工单；可为自己的订单创建工单；可查看和补充自己未关闭工单的消息；不能指定客服、优先级、状态或修改他人工单。
   - `AGENT`：可查看所有处于 `MANUAL_REVIEW` 的工单；仅可接取未分配工单；仅可回复、解决、关闭自己已接取的工单；不能处理其他客服已接取的工单。
   - `ADMIN`：可查看全量数据、管理售后策略和统计；可为工单分配/改派客服及执行关闭操作，但不得绕过状态机直接修改状态。
   - 所有归属和权限判断只以当前 Token 对应的用户身份为准；前端隐藏按钮不是授权依据。

2. **状态定义与默认值**

   | 状态 | 含义 | 是否可继续发送消息 | 默认处理人 |
   | --- | --- | --- | --- |
   | `AI_PROCESSING` | 工单已创建，等待 AI 自动处理或分类 | 用户可以 | 无 |
   | `MANUAL_REVIEW` | 需要人工处理，可能尚未被客服接取 | 用户、已接单客服可以 | 无或已接单客服 |
   | `RESOLVED` | 客服或受控 AI 已给出解决结果，等待关闭 | 用户可以补充；客服不再普通回复 | 已接单客服 |
   | `CLOSED` | 流程结束的终态 | 不可以 | 保留最后处理客服 |

   - 创建工单的初始状态固定为 `AI_PROCESSING`，默认分类为 `OTHER`、默认优先级为 `MEDIUM`。
   - `agentId` 只能由接单、管理员分配/改派操作写入；客户端不得通过创建/更新请求直接指定。

3. **允许的状态转换与动作**

   | 动作 | 执行者 | 状态变化 | 必须满足的条件 |
   | --- | --- | --- | --- |
   | 创建工单 | `USER` | `无 -> AI_PROCESSING` | 订单存在且归当前用户；标题、描述有效。 |
   | AI 转人工 | 系统业务服务 | `AI_PROCESSING -> MANUAL_REVIEW` | AI 无法处理、命中策略或用户明确要求人工。 |
   | AI 解决 | 系统业务服务 | `AI_PROCESSING -> RESOLVED` | 命中允许自动解决的策略；不得由模型直接写数据库。 |
   | 接单 | `AGENT` | `MANUAL_REVIEW -> MANUAL_REVIEW` | `agentId` 为空；以原子更新防止两位客服同时接单。 |
   | 客服回复 | 已接单 `AGENT` | 保持 `MANUAL_REVIEW` | 工单属于当前客服。 |
   | 用户补充 | 工单所属 `USER` | `AI_PROCESSING` 保持；`MANUAL_REVIEW` 保持；`RESOLVED -> MANUAL_REVIEW` | 关闭工单禁止补充；已解决后补充视为重新请求人工确认。 |
   | 标记解决 | 已接单 `AGENT` 或 `ADMIN` | `MANUAL_REVIEW -> RESOLVED` | 至少存在一条客服回复或可追溯的策略/AI 解决记录。 |
   | 关闭工单 | 已接单 `AGENT` 或 `ADMIN` | `RESOLVED -> CLOSED` | 必须已解决；写入关闭时间和操作日志。 |

   除表中“用户补充”允许的 `RESOLVED -> MANUAL_REVIEW` 外，禁止逆向、跳级或直接写入任意状态；冲突操作返回 `409`。

4. **消息、日志与审计**
   - 每次创建、接单、改派、发送消息、状态变化、SLA 预警/升级、AI 工具调用均须记录操作日志：`ticketId`、动作、操作人 ID/角色、变更前后状态、时间、必要说明。
   - 消息记录须包含 `ticketId`、发送者 ID、发送者角色、内容、发送时间和消息来源（`USER`、`AGENT`、`AI`、`SYSTEM`）。AI 回复必须保存为消息，但 AI 不拥有绕过业务服务改变工单状态的权限。
   - 用户及客服只能查看自己有权访问工单的消息、日志摘要；管理员可以查看完整审计信息。日志不可由普通接口修改或删除。

5. **SLA 与策略的最小规则**
   - 创建工单时按优先级计算默认 SLA：`LOW` 72 小时、`MEDIUM` 48 小时、`HIGH` 24 小时、`URGENT` 4 小时；已启用且匹配的售后策略可以覆盖默认时长。
   - 距截止时间不足总时长的 25% 时标记 `slaWarning=1`；超过截止时间标记 `slaEscalated=1`，并将优先级提升一级（`URGENT` 不再提升），同时写入日志。
   - 策略只可决定自动回复、允许 AI 解决或转人工；任何影响订单、退款、账号或数据库写入的动作均须由后端权限和业务规则再次校验。

6. **演示验收数据**
   - A 维护至少一个普通用户、一个客服、一个管理员账号，以及每个普通用户至少一笔可用于创建工单的自有订单。
   - C 维护至少一条可转人工的工单、一个已接单工单、一个已解决工单和一个已关闭工单；B 使用这些数据完成页面联调与验收截图。

### 2.3 协作与提交约定

- [ ] 使用功能分支开发；提交信息写明模块和动作，例如 `feat(ticket): add claim endpoint`。（全员）
- [ ] 不提交 `node_modules`、`target`、压缩包、真实 API Key、个人 IDE 临时文件。（全员）
- [ ] 每项任务完成前至少执行相关构建、接口测试或页面操作测试，并在本表勾选时注明结果。（全员）
- [ ] 发生接口、数据库表或状态机变更前，先在组内同步；避免 A 与 C 同时修改同一个后端类。（全员）

## 3. 依赖标记说明

| 标记 | 含义 | 执行要求 |
| --- | --- | --- |
| **已完成** | 已有可核对的正式约定或交付物 | 不再阻塞其他任务；后续仅按其约定实施。 |
| **可独立开发** | 不依赖其他成员的未完成代码；第 2.1、2.2 的正式约定已满足 | 可立即开始、测试并独立提交。 |
| **需接口实现/联调** | 接口字段、状态和权限规则已经确定，但提供方代码尚未完成 | 可立即完成 DTO、Mock、页面、单元测试；真实请求、完整验收和联调代码需等待提供方接口。 |
| **必须等待** | 功能本身必须使用另一项已运行的业务能力，缺少它无法判断或验证正确性 | 可进行设计和 Mock，但在前置任务勾选完成、提供验收结果前，不实现真实业务写入或合并正式联调逻辑。 |

## 4. 成员 A：后端平台、账号权限与订单模块

**责任边界**：账号、认证、权限、订单与公共后端规范。A 不直接改动 C 负责的工单流转、消息、策略和 AI 业务实现；若需公共 DTO 或权限变更，应先同步 C。

| 进度 | 任务 | 依赖标记 | 前置依赖 / 联调对象 | 完成标准 |
| --- | --- | --- | --- | --- |
| - [ ] | 统一登录、创建类接口的 JSON 请求体与统一响应格式 | 可独立开发 | 联调：B、C | 前端 JSON 请求可被后端正确接收，错误响应格式一致。 |
| - [ ] | 完成登录、退出、注册、个人资料查询/修改 | 可独立开发 | 联调：B | 三类演示账号可登录，Token 与用户信息正确返回。 |
| - [ ] | 密码改用 BCrypt 加密存储和校验 | 可独立开发 | 无 | 数据库不保存明文密码，旧演示账号可迁移或重置。 |
| - [ ] | 完善 Sa-Token 未登录、无权限、登录失效的统一异常响应 | 可独立开发 | 联调：B | 401/403 可被前端正确识别与提示。 |
| - [ ] | 完成角色、菜单/API 权限和数据范围规则 | 可独立开发 | 按第 2.2 节实现；联调：C、B | 用户、客服、管理员的越权请求被拒绝。 |
| - [ ] | 修复订单分页、订单详情，并限制用户仅查询自己的订单 | 可独立开发 | 联调：B | 订单分页类型、详情、归属校验正确。 |
| - [ ] | 创建工单时校验订单存在且属于当前用户 | 可独立开发 | 按第 2.1、2.2 节实现；与 C 协调接口落点 | 非本人订单不能创建工单。 |
| - [ ] | 整理 SQL、样例数据、环境变量配置、启动说明 | 可独立开发 | 提供给：B、C | 新成员按文档可启动后端和导入数据。 |
| - [ ] | 发布接口文档与 Postman/Apifox 集合 | 需接口实现/联调 | 按第 2.1、2.2 节编写；联调：B、C | 已实现核心接口都有请求、响应、权限、异常示例。 |
| - [ ] | 为认证、订单、归属校验编写关键测试 | 可独立开发 | 无 | 测试可重复运行并覆盖异常场景。 |

**A 的关键交付物**：可用认证接口、订单接口、角色/权限规则、样例数据、接口文档。

## 5. 成员 B：前端基础、用户端工单体验与数据展示模块

**责任边界**：前端工程、请求层、登录态、Layout、用户订单与工单列表/创建页、通用展示页面。B 不修改 C 的工单详情、消息动作、AI 页面业务逻辑；可提供通用组件和页面容器。

| 进度 | 任务 | 依赖标记 | 前置依赖 / 联调对象 | 完成标准 |
| --- | --- | --- | --- | --- |
| - [ ] | 安装依赖，修复并验证 `npm run build` 与 TypeScript 问题 | 可独立开发 | 无 | 前端生产构建通过。 |
| - [ ] | 完善 Axios：超时、Token 注入、401 清理、错误提示 | 可独立开发 | 联调：A | 所有业务请求经 `/api` 代理并自动携带 Token。 |
| - [ ] | 完成 Pinia 用户状态持久化和路由守卫 | 需接口实现/联调 | 按第 2.1 节字段开发；联调：A 的登录接口 | 刷新后登录态可恢复；未登录不能访问业务页。 |
| - [ ] | 统一 API TypeScript 类型，修复订单分页和工单 ID 类型问题 | 可独立开发 | 按第 2.1、2.2 节编写；与 A、C 同步已实现字段 | `R<Page<Orders>>` 等类型与正式约定一致。 |
| - [ ] | 完成后台 Layout、菜单、Header、面包屑与角色菜单显示 | 可独立开发 | 按第 2.1、2.2 节角色/权限规则实现 | 未实现菜单隐藏或标明，路径不再失效。 |
| - [ ] | 完成订单列表、订单详情及“从订单创建工单”入口 | 需接口实现/联调 | 可先 Mock；真实联调：A 的订单接口 | 用户只能看到自己的订单，并可带入订单创建工单。 |
| - [ ] | 完成工单列表、分页、筛选、状态显示与空/错/加载状态 | 需接口实现/联调 | 可先 Mock；真实联调：C 的工单列表接口 | 列表数据、分页和状态中文映射正确。 |
| - [ ] | 完成创建工单表单、校验与提交后的跳转/刷新 | 需接口实现/联调 | 可先完成表单；真实联调：A 的订单归属校验、C 的创建工单接口 | 创建成功后能在列表和详情中看到首条消息。 |
| - [ ] | 提供工单详情、消息、AI 页面的路由入口、通用消息样式和错误组件 | 可独立开发 | 提供给：C | C 可直接接入页面，不需改动 Layout。 |
| - [ ] | 完成工作台与基础管理统计展示 | 需接口实现/联调 | 可先完成布局；真实联调：A/C 提供统计接口或统计字段 | 数据真实或明确标注为暂未接入，不保留误导性静态数据。 |

**B 的关键交付物**：可构建前端、可靠登录态与路由、订单/创建/列表闭环页面、可复用 UI 基础。

## 6. 成员 C：工单流转、售后策略、AI 客服与验收模块

**责任边界**：工单详情、消息、接单/解决/关闭、状态机、策略/SLA、AI 服务和端到端验收。C 是工单相关后端实现的主负责人；涉及登录、权限公共规则和订单归属时与 A 协作，不直接覆盖 A 的账号/订单代码。

| 进度 | 任务 | 依赖标记 | 前置依赖 / 联调对象 | 完成标准 |
| --- | --- | --- | --- | --- |
| - [x] | 定义工单状态机、角色动作和状态转换表（2026-07-27，见第 2.2 节） | 已完成 | 已作为正式约定提供给：A、B | 状态、触发人、允许转换和失败提示已形成正式文档。 |
| - [ ] | 修复工单 DTO 校验、ID 类型、null 安全更新等基础问题 | 可独立开发 | 与 A 同步统一响应/JSON 规范 | 创建、消息、更新接口参数校验正确。 |
| - [ ] | 完成工单详情和消息列表 API | 可独立开发 | 按第 2.1、2.2 节实现；联调：B | 用户不能读取他人工单，详情含正确消息和操作人。 |
| - [ ] | 完成发送消息、接单、解决、关闭及操作日志 API | 可独立开发 | 按第 2.2 节状态转换实现；联调：A、B | 每次操作校验角色、状态与归属，并留下可追溯日志。 |
| - [ ] | 完成工单详情页：加载详情、消息列表、回复和状态动作 | 需接口实现/联调 | 可先完成页面和 Mock；真实联调：C 的消息/状态动作 API、B 的页面容器 | 客服操作后用户刷新可见消息和最终状态。 |
| - [ ] | 完成售后策略管理、规则匹配与 SLA 截止时间/预警展示 | 需接口实现/联调 | 可先完成策略模型；真实联调：C 的工单更新 API，联调：B、A | 至少一条策略可匹配不同类别工单并显示 SLA。 |
| - [ ] | 搭建 Spring AI Alibaba/百炼配置，完成普通对话接口 | 可独立开发 | 参考 `springAIDemo/`；联调：B | API Key 仅来自环境变量，普通对话可稳定回复。 |
| - [ ] | 完成 AI 对话页面、流式 SSE、停止生成和异常处理 | 需接口实现/联调 | 可先完成页面和 SSE 消费；真实联调：C 的普通/流式 AI 接口、B 的页面容器 | 前端可逐字显示、可停止并正确处理异常。 |
| - [ ] | 将订单/工单/消息上下文接入 AI，完成转人工/生成工单 | 必须等待 | 前置：A 的订单归属与 C 的工单流转均完成 | AI 只读取有权数据；复杂问题可受控转人工。 |
| - [ ] | 实现只读 Tool Calling、FAQ 管理和关键词检索 | 必须等待 | 前置：AI 普通对话、订单/工单/策略接口稳定 | 模型不能直接写库或越权查询。 |
| - [ ] | 编写三角色端到端用例、问题清单和流程图 | 必须等待 | 前置：A/B/C 核心闭环已联调 | 测试可复现“创建 -> 接单 -> 回复 -> 解决 -> 关闭”。 |

**C 的关键交付物**：可追溯工单闭环、策略/SLA、受控 AI 能力、端到端验收材料。

## 7. 仍需等待实现的关键链路

第 2.1、2.2 的协议和状态机已经确定，因此不再有“等待约定”的阻塞项。以下不是全项目的停工点；未列出的任务均可按上表并行推进。标记为“必须等待”的工作，可以做设计、Mock 与页面骨架，但在前置项勾选完成并提供可验证结果前，不实现真实业务写入或合并正式联调逻辑。

| 后续任务 | 必须等待的前置完成项 | 原因 |
| --- | --- | --- |
| C：AI 上下文、转人工、生成工单 | A：订单归属与权限实现；C：工单流转实现 | 该功能会读取用户业务数据并写入工单，必须建立在实际权限与状态流转代码已验证的基础上。 |
| C：只读 Tool Calling、FAQ 管理与关键词检索 | C：普通 AI 对话、订单/工单/策略接口稳定 | 工具必须基于已实现的只读业务服务，不能连接不稳定或未授权的数据源。 |
| B：真实工作台统计 | A/C：统计口径、统计接口或字段 | 页面结构可先完成；真实数据展示和验收必须等待统计数据来源。 |
| C：三角色端到端验收 | A、B、C：核心闭环已联调 | 没有真实的登录、订单、创建、接单、回复、解决、关闭链路，无法获得有效验收结论。 |

## 8. 可并行推进的推荐顺序

```text
立即并行
├─ A：认证/订单/JSON 契约实现/接口文档
├─ B：构建修复/请求层/路由守卫/Layout/页面壳
└─ C：状态机代码/工单 DTO 修复/详情与消息接口实现

第一轮联调
├─ A -> B：登录、订单、创建工单接口
└─ C -> B：工单列表、详情、消息、状态动作接口

第二轮并行
├─ B：真实页面联调、列表/详情/工作台
├─ C：策略/SLA、普通 AI 对话
└─ A：权限加固、测试、数据与接口维护

最终联调
└─ C 组织三角色端到端验收；A、B 分别修复后端和前端问题
```

## 9. 每次联调前检查

- [ ] 提供方已在本地完成接口测试，并给出示例请求/响应和账号/测试数据。
- [ ] 使用方已同步最新分支、接口文档和 TypeScript 类型。
- [ ] 前端已确认代理地址、Token Header 和错误处理。
- [ ] 后端已确认数据库脚本、环境变量和权限配置。
- [ ] 本次联调覆盖正常路径、未登录、无权限、空数据和非法参数。
- [ ] 联调问题已记录责任人、复现步骤、优先级与修复提交号。
